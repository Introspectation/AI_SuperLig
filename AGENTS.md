# Agent instructions

Before changing this repository, read `HANDOFF.md` completely and reverify its
branch, worktree, PR/CI, current-date, and live-data-freshness claims. Current
observable state overrides the handoff.

Follow the repository contracts linked from `HANDOFF.md`. Do not infer approval
to merge, publish, acquire external data, or broaden modeling scope. At a real
agent-transfer checkpoint, update `HANDOFF.md` in place if that update and its
Git operations are authorized.
